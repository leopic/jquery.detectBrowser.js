Basic jQuery plugin for User Agent Spoofing.

It is not intended to be a replacement for
Peter-Paul Koch's browser detect function,
but much rather a compact plugin that only
considers most used browsers.

Support:
- Firefox
- Chrome
- Safari
- IE
